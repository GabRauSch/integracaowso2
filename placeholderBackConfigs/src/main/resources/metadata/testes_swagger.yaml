openapi: 3.0.1
info:
  title: testes
  description: API Definition of testes
  version: 1.0.0
servers:
- url: http://localhost:8290/testes
paths:
  /{userId}:
    get:
      parameters:
      - name: userId
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
